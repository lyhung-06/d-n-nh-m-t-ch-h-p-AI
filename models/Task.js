const {read,write,id}=require('../config/database');module.exports={all:()=>read().tasks,find:i=>read().tasks.find(x=>x.id==i),byProject:i=>read().tasks.filter(x=>x.projectId==i),create:d=>{const db=read(),t={id:id(db,'tasks'),title:d.title,description:d.description||'',projectId:+d.projectId,assigneeId:+d.assigneeId,priority:d.priority||'MEDIUM',status:d.status||'TODO',deadline:d.deadline||''};db.tasks.push(t);write(db);return t},update:(i,d)=>{const db=read(),t=db.tasks.find(x=>x.id==i);if(!t)return null;Object.assign(t,d);if(d.projectId)t.projectId=+d.projectId;if(d.assigneeId)t.assigneeId=+d.assigneeId;write(db);return t},remove:i=>{const db=read(),n=db.tasks.findIndex(x=>x.id==i);if(n<0)return false;db.tasks.splice(n,1);write(db);return true}};
