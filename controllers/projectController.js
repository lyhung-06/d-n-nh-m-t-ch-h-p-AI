const P=require('../models/Project'),U=require('../models/User'),{recalculate}=require('../services/projectService');const e=p=>({...p,owner:U.find(p.ownerId)});exports.list=(q,s)=>s.json(P.all().map(e));exports.create=(q,s)=>s.status(201).json(e(P.create(q.body,q.session.user.id)));exports.update=(q,s)=>{const p=P.update(+q.params.id,q.body);p?s.json(e(p)):s.status(404).json({message:'Không tìm thấy dự án.'})};exports.remove=(q,s)=>P.remove(+q.params.id)?s.json({ok:true}):s.status(404).json({message:'Không tìm thấy dự án.'});exports.recalculate=(q,s)=>s.json(e(recalculate(+q.params.id)));
