controllers: xử lý request và response
models: dữ liệu và CRUD
routes: API endpoints
middleware: đăng nhập và phân quyền
services: nghiệp vụ dùng chung và AI
config: cấu hình database JSON
database: dữ liệu hệ thống
public: giao diện HTML/CSS/JS
